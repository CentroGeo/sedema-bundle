# Entorno de desarrollo local para conafor (frontends fuera de Docker)
# Requiere: 127.0.0.1 conafor.localhost en /etc/hosts
hostname=conafor.localhost
env_type=dev
oidc_provider_url=http://conafor.localhost/iam/realms/conafor
https_mode=http
