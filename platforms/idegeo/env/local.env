# Entorno de desarrollo local para idegeo (frontends fuera de Docker)
# Requiere: 127.0.0.1 idegeo.localhost en /etc/hosts
hostname=idegeo.localhost
env_type=dev
oidc_provider_url=http://idegeo.localhost/iam/realms/idegeo
https_mode=http
